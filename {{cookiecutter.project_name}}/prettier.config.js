/**
 * @see https://prettier.io/docs/configuration
 * @type {import('prettier').Config}
 */
const config = {
  trailingComma: "es5",
  useTabs: false,
  tabWidth: 2,
  printWidth: 100,
  semi: true,
  singleQuote: true,
  plugins: [],
  overrides: [],
};

export default config;
